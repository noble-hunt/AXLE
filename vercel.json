{
  "buildCommand": "npm exec tsc -- -p tsconfig.server.json && npm run build",
  "functions": {
    "api/[...slug].ts": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "{server,shared}/**/*.js"
    }
  }
}
