{
  "buildCommand": "npm exec tsc -- -p tsconfig.server.json --noEmitOnError false && npm run build",
  "outputDirectory": "dist",
  "functions": {
    "api/[...slug].ts": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "{server,shared}/**/*.js"
    }
  }
}
